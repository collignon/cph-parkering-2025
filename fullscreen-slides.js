// Simplified callback for Danish urbanism stats
function showParadoxSlide() {
    // Show the Danish urbanism stats
    if (typeof danishUrbanism !== 'undefined') {
        danishUrbanism.addToChapter('dansk-paradoks');
    }
} 